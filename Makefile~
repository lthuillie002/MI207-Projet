all :
	gcc client.c utils.c -o client -std=gnu99
	gcc control.c utils.c -o control -std=gnu99

client :
	gcc client.c utils.c -o client -std=gnu99

control :
	gcc control.c utils.c -o control -std=gnu99

clean :
	rm -rf *.o

remove :
	rm -rf hello
